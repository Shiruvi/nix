{
programs.nixvim.plugins.mini-animate = {
enable = true;
settings = {
	close = {
    enable = true;
  };
  cursor = {
    enable = false;
  };
  open = {
    enable = true;
  };
  resize = {
    enable = true;
  };
  scroll = {
    enable = true;
  };
	};
};
}
